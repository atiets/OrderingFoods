<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.OrderingFoods" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="CustomSpinnerStyle" parent="Widget.AppCompat.Spinner">
        <!-- Màu nền khi Spinner được xổ xuống -->
        <item name="android:popupBackground">#4C4FFF</item>

        <!-- Màu viền của Spinner -->
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>

        <!-- Kích thước chữ và màu sắc của TextView bên trong Spinner -->
        <item name="android:textColor">#333333</item>
        <item name="android:textSize">16sp</item>

        <!-- Padding cho nội dung bên trong Spinner -->
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>

        <!-- Hiệu ứng khi người dùng chọn một item -->
        <item name="android:spinnerMode">dropdown</item>
        <item name="android:dropDownVerticalOffset">8dp</item>
        <item name="android:dropDownHorizontalOffset">8dp</item>

        <!-- Điều chỉnh chiều cao của mục khi xổ xuống -->
        <item name="android:dropDownHeight">wrap_content</item>

        <!-- Điều chỉnh padding cho các item trong danh sách xổ xuống -->
        <item name="android:dropDownWidth">match_parent</item>

        <!-- Màu chữ khi danh sách xổ xuống -->
        <item name="android:popupElevation">4dp</item>
        <item name="android:dividerHeight">1dp</item>
        <item name="android:divider">#FF0000</item> <!-- Màu chia cách các mục trong danh sách -->
    </style>


    <style name="Theme.OrderingFoods" parent="Base.Theme.OrderingFoods" />
</resources>